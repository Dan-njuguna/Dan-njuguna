name: 🔁 Update Daily Quote

on:
  schedule:
    - cron: "0 0 * * *"  # Daily at 00:00 UTC
  workflow_dispatch:

jobs:
  update-quote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Fetch quote from API
        id: get-quote
        run: |
          curl -s https://zenquotes.io/api/today > quote.json
          echo "QUOTE=$(jq -r '.content + \" — \" + .author' quote.json)" >> $GITHUB_ENV

      - name: Update README with new quote
        run: |
          sed -i "/<!-- QUOTE_START -->/,/<!-- QUOTE_END -->/c\<!-- QUOTE_START -->\n> ${QUOTE}\n<!-- QUOTE_END -->" README.md

      - name: Push changes
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}
          git add .
          git commit -m "🔁 update daily tech quote" || echo "No changes to commit"
          git push

